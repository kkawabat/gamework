name: Deploy Signaling Server to Droplet

on:
  push:
    paths:
      - 'signaling-server/**'
      - '.github/workflows/deploy-signaling-server.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Deploy to Droplet
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.DROPLET_HOST }}
        username: ${{ secrets.DROPLET_USER }}
        key: ${{ secrets.DROPLET_SSH_KEY }}
        port: ${{ secrets.DROPLET_PORT || 22 }}
        script: |
          set -e
          
          echo "🚀 Starting GameWork Signaling Server Deployment via GitHub Actions"
          echo "=================================================================="
          
          # Download and run the deployment script
          print_status() {
              echo -e "\033[0;32m[INFO]\033[0m $1"
          }
          
          print_status "Ensuring we have the latest code (handles force pushes)..."
          cd /opt/gamework
          git fetch --all
          git reset --hard origin/main
          
          print_status "Downloading deployment script..."
          curl -fsSL https://raw.githubusercontent.com/kkawabat/gamework/main/signaling-server/deploy.sh -o /tmp/deploy.sh
          chmod +x /tmp/deploy.sh
          
          print_status "Running deployment script..."
          /tmp/deploy.sh
          
          print_status "✅ GitHub Actions deployment completed!"
          print_status "Container name: gamework-signalling-server"
          print_status "Network: app_network"
          print_status "Ready for Caddy reverse proxy configuration!"
